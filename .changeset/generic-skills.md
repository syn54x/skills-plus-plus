---
"mattpocock-skills": minor
---

Five general-purpose skills move in from `syn54x/skills`: `scaffold-python-project`, `scaffold-frontend-project` and `prepare-release-notes` in `engineering/`, and `adhd` and `eli5` in `productivity/`. The scaffolds' opt-in flag is now `syn54x-skills`, and their last step hands off to `/setup-syn54x-skills` for the user to run instead of reading that skill's `SKILL.md` and following it by hand, which broke the invocation rules.
