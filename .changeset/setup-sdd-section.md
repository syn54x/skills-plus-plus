---
"mattpocock-skills": minor
---

`setup-syn54x-skills` gains an optional **Section D: SDD pipeline**, offered when the tracker is GitHub. It checks `gh` 2.94+, creates the pipeline's labels plus the five canonical triage labels, offers the `Epic` and `Task` issue types on org repos, writes an `<!-- sdd-routing -->` block below `## Agent skills` (replaced in place on re-run), and can copy the `sdd-implement.yml` and `sdd-review.yml` cloud workflows. This replaces the separate setup skill in `syn54x/skills`, which could only tell the user to run the upstream setup first.
