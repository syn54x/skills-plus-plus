---
"mattpocock-skills": patch
---

`ask-syn54x` routes the SDD flow (epic, hardened sub-issues, `build-epic` or the cloud workflow by size, you merge, `close-epic`) and the new standalones (`adhd`, `eli5`, `prepare-release-notes`, the scaffolds), and drops a stale line saying `diagnosing-bugs` hands off to `improve-codebase-architecture`. `README.md` gains the SDD flow, the size ladder, and the rationale and credits carried over from `syn54x/skills`; `CONTEXT.md` gains the SDD and scaffold glossary.
