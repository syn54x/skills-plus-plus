---
"mattpocock-skills": minor
---

Fork identity for `syn54x/skills-plus-plus`. The plugin is now `skills-plus-plus` in the `syn54x` marketplace (`/plugin marketplace add syn54x/skills-plus-plus`, then `/plugin install skills-plus-plus@syn54x`). `setup-matt-pocock-skills` is renamed `setup-syn54x-skills` and `ask-matt` is renamed `ask-syn54x`; every reference, docs page, and link follows. New: ADR 0003 (why the fork extends upstream in place) and `.agents/upstream-sync.md` (how to merge from upstream).
